# Database connection string (FastAPI defaults to SQLite)
DATABASE_URL=sqlite:///pinple.db

# Running environment (development / production)
ENVIRONMENT=development

# CORS Allowed Origins (comma separated list for CORS middleware)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
